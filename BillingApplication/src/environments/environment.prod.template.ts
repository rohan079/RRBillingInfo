// Replaced at build time from NG_APP_* env vars (see package.json "build" script).
export const environment = {
  production: true,
  apiBaseUrl: '@@NG_APP_API_BASE_URL@@',
  appDisplayName: '@@NG_APP_DISPLAY_NAME@@',
};
