export const environment = {
  production: false,
  /** Spring Boot API (see BillingSpringBoot CORS for localhost:4200). */
  apiBaseUrl: 'http://localhost:8080',
  /** Navbar brand and browser tab title (must match app.display-name in Spring Boot). */
  appDisplayName: 'RR Silks Inventory Info',
};
